name: admob_adsense_dashboard
description: 애드몹 & 애드센스 수익 대시보드 - 한국인 전용

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.2.1
  http: ^1.2.0
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  fl_chart: ^0.68.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
